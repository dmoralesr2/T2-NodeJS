# T2-NodeJS
Orden para correr este proyecto:
  1. Entrar en la carpeta del repositorio ```cd T2-NodeJS```.
  2. Instalar modulos ```npm install```.
  3. Correr el proyecto ```npm start```.
  4. URL de INICIO ```http://localhost:5000/estudiante```.
